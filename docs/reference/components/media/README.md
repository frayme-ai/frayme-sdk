# Media

13 components in this group.

| Component | Description |
|---|---|
| [AudioPlayer](audio-player.md) | A native HTML5 audio player in a compact card (title + browser controls). An invalid/absent `src` shows a mute |
| [Carousel](carousel.md) | Horizontal scroll-snap strip of simple cards (`items` as [{title, description, image?, icon?}]) with optional  |
| [Figure](figure.md) | A semantic figure: an image with a muted caption (and optional credit). An invalid/absent `src` shows an aspec |
| [Gallery](gallery.md) | A responsive image grid with an optional built-in lightbox. Set `columns` for the grid width and `ratio` for t |
| [Image](image.md) | Image component: renders an img tag when `src` is set, otherwise a muted placeholder box showing the `alt` tex |
| [Lightbox](lightbox.md) | A standalone full-screen media overlay (role=dialog). Prev/next arrows change the current image (internal stat |
| [Marquee](marquee.md) | An auto-scrolling marquee strip. Supply `items` (escaped text) or children; the content is duplicated for a se |
| [MediaAnnotator](media-annotator.md) | Annotate an image with pins, boxes, text notes, and freehand ink. Author an image + optional pre-placed marks; |
| [MediaGrid](media-grid.md) | A responsive grid of media tiles for galleries, logos, or link cards. Display-only (no lightbox). A tile with  |
| [MediaScrubber](media-scrubber.md) | A visual media scrubber: a waveform / bar / line track with a draggable playhead, played + buffered fills, cha |
| [Thumbnail](thumbnail.md) | A small fixed-size preview image (object-cover) for lists and cards. An invalid/absent `src` falls back to a m |
| [VideoPlayer](video-player.md) | A native HTML5 video player (browser controls only, no custom UI, no capture). Aspect-locked frame; an invali |
| [YouTube](you-tube.md) | A lightweight YouTube card: the video thumbnail with a play-button overlay that LINKS OUT to youtube.com in a  |

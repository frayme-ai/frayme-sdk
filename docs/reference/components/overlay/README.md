# Overlay

9 components in this group.

| Component | Description |
|---|---|
| [Backdrop](backdrop.md) | A dimming scrim layered over its children, toggled by `active`, the generic underlay for a modal/drawer/loadi |
| [CommandPalette](command-palette.md) | Cmd-k fuzzy action launcher rendered as an INLINE command surface (no portal, SSR-safe). Renders a search head |
| [Confirmation](confirmation.md) | An inline human-in-the-loop gate: a message + an Approve button (emits `commit`) and a Deny button (emits `dis |
| [Dialog](dialog.md) | Modal dialog: a centered panel over a backdrop scrim with a title/description header, × close, and children as |
| [Drawer](drawer.md) | Edge-docked sheet (bottom by default; left/right/top via `side`). Set openPath to a boolean state path. Use se |
| [HoverCard](hover-card.md) | A trigger (text label or wrapped children) that reveals a rich preview card, title + description + optional a |
| [Popover](popover.md) | Click-triggered floating panel showing `content` next to a `trigger` button. Light-dismiss: clicking outside t |
| [Toggletip](toggletip.md) | A click-to-reveal info bubble anchored to a small trigger button, the accessible, keyboard-dismissable altern |
| [Tooltip](tooltip.md) | Small hover/focus bubble showing `content` next to its `text` trigger. Use for a brief clarifying hint on a la |
